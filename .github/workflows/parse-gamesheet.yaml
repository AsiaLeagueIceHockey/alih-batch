name: Legacy Game Sheet Scraper (Blocked pending 2026-27 source)

on:
#  schedule:
#    - cron: '*/20 * * * *'
  
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Block unsafe legacy writer
        run: |
          echo "2026-27 game-sheet source mapping is not verified."
          echo "This workflow intentionally performs no Supabase writes."
          exit 1
